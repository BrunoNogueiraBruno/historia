import { LatamMapContainer } from '../../styles/components/latamMap'
import BaseMap from './BaseMap'

const LatamMap = () => {

    return (
        <LatamMapContainer>
            <BaseMap />
        </LatamMapContainer>
    )
}

export default LatamMap
